# Device configuration
# Options: cpu, cuda (for NVIDIA GPU), mps (for Apple Silicon)
BACKBONE_DEVICE=cpu
CODEC_DEVICE=cpu

# Note: The service runs on port 8080 externally (configured in docker-compose.yml)
# Internal container port is 8000
